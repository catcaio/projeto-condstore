Tenant isolation scan baseline
==============================

This file documents the baseline for the tenant isolation scanner.
It is intentionally kept as an evergreen artifact and MUST NOT contain
a stale list of past violations or raw scanner output.

Current expected baseline
-------------------------

Found 0 violations.

How to run the scanner
----------------------

1. Run the tenant isolation scanner from the project root:
   - (Document the exact command here, e.g. `pnpm tenant-scan` or similar.)
2. Review the scanner output locally:
   - If any violations are reported, fix them in the codebase.
   - Re-run the scanner until it reports `0 violations`.
3. Do NOT commit the full scanner output file.
   - Update this file only if the expected baseline changes
     (for example, if the scanner itself changes and introduces
     new categories of checks).

Notes
-----

- This file is versioned to track the intended tenant isolation
  posture, not to store transient scan results.
- If you are unsure how to interpret a scanner finding, reach out
  to the security or platform team for guidance before merging.
