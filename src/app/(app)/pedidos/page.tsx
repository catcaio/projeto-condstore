import { WorkspaceFoundationPage } from '@/modules/workspace/foundation';

export const metadata = {
    title: 'Pedidos — CONDSTORE OS',
};

export default function PedidosPage() {
    return <WorkspaceFoundationPage moduleId="pedidos" />;
}
