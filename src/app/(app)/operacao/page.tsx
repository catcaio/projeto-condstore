import { WorkspaceFoundationPage } from '@/modules/workspace';

export const metadata = {
    title: 'Operacao — CONDSTORE OS',
};

export default function OperacaoPage() {
    return <WorkspaceFoundationPage moduleId="operacao" />;
}
