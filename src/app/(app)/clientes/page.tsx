import { WorkspaceFoundationPage } from '@/modules/workspace/foundation';

export const metadata = {
    title: 'Clientes — CONDSTORE OS',
};

export default function ClientesPage() {
    return <WorkspaceFoundationPage moduleId="clientes" />;
}
