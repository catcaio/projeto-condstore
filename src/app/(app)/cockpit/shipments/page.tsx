import { ShipmentsClient } from './shipments.client';

export const metadata = {
    title: 'Embarques (Etiquetas) - Cockpit',
};

export default function ShipmentsPage() {
    return <ShipmentsClient />;
}
