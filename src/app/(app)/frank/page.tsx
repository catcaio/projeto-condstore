import { WorkspaceFoundationPage } from '@/modules/workspace/foundation';

export const metadata = {
    title: 'Frank — CONDSTORE OS',
};

export default function FrankPage() {
    return <WorkspaceFoundationPage moduleId="frank" />;
}
