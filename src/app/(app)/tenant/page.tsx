import { WorkspaceFoundationPage } from '@/modules/workspace/foundation';

export const metadata = {
    title: 'Tenant — CONDSTORE OS',
};

export default function TenantPage() {
    return <WorkspaceFoundationPage moduleId="tenant" />;
}
