import { WorkspaceFoundationPage } from '@/modules/workspace';

export const metadata = {
    title: 'Metricas — CONDSTORE OS',
};

export default function MetricasPage() {
    return <WorkspaceFoundationPage moduleId="metricas" />;
}
