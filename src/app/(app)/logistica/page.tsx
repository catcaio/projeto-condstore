import { WorkspaceFoundationPage } from '@/modules/workspace';

export const metadata = {
    title: 'Logistica — CONDSTORE OS',
};

export default function LogisticaPage() {
    return <WorkspaceFoundationPage moduleId="logistica" />;
}
