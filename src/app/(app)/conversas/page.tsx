import { WorkspaceFoundationPage } from '@/modules/workspace';

export const metadata = {
    title: 'Conversas — CONDSTORE OS',
};

export default function ConversasPage() {
    return <WorkspaceFoundationPage moduleId="conversas" />;
}
