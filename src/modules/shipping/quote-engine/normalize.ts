import { NormalizedQuote } from '../carriers/types';

/**
 * Normalizes quotes from different carriers into a standard format.
 * TODO: Implement normalization logic per carrier schema when carriers are integrated.
 */
export function normalizeQuotes(rawQuotes: any[]): NormalizedQuote[] {
    // Stub implementation
    return [];
}
