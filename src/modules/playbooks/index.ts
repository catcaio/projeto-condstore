/**
 * Playbooks Module — Client Entrypoint.
 *
 * Re-exports public UI components for consumers outside the playbooks module.
 * Use `@/modules/playbooks` to import these.
 */

export { PlaybookEditor } from './components/playbook-editor';
