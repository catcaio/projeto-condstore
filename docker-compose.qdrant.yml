version: '3.9'
services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD", "pidof", "qdrant"]
      interval: 10s
      timeout: 5s
      retries: 5

