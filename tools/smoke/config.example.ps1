$BASE_URL = "https://staging.example.com"
$INTERNAL_TOKEN = "REPLACE_ME"
$TENANT_ID = "tenant-id-if-needed"
# Optional fake value to validate seed blocking. Never use a real seed token here.
$SEED_TOKEN = "DO_NOT_USE_REAL"
# Optional endpoint only (no credentials).
$TWILIO_WEBHOOK_URL = "https://staging.example.com/api/webhook"
