# APP
NODE_ENV=development
APP_URL=http://localhost:3000

# DATABASE (TiDB / MySQL)
# Format: mysql://USER:PASSWORD@HOST:PORT/DB_NAME?ssl={"rejectUnauthorized":true}
DATABASE_URL=mysql://*:*@*:*/*?ssl={"rejectUnauthorized":true}

# TWILIO
TWILIO_ACCOUNT_SID=AC*
TWILIO_AUTH_TOKEN=*
TWILIO_WHATSAPP_NUMBER=whatsapp:+*

# Webhook signature verification
# Set to the public base URL of your deployment (e.g. https://yourapp.vercel.app).
# Must match EXACTLY the URL configured in the Twilio console (no trailing slash).
# If omitted, the URL is reconstructed from x-forwarded-proto/x-forwarded-host headers.
# Required in production to prevent webhook spoofing.
TWILIO_WEBHOOK_BASE_URL=https://your-app.example.com

# Set to "false" ONLY for local dev (disables HMAC validation entirely).
# NEVER set "false" in production.
TWILIO_SIGNATURE_VALIDATION_ENABLED=true

# NGROK (for local webhook testing)
NGROK_URL=https://*.ngrok-free.dev

# CACHE (Optional)
# UPSTASH_REDIS_REST_URL=*
# UPSTASH_REDIS_REST_TOKEN=*
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
RATE_LIMIT_DEFAULT_WINDOW_SECONDS=60
RATE_LIMIT_DEFAULT_MAX=60
IDEMPOTENCY_TTL_SECONDS=86400

# AUTH (JWT session signing — required in production; fallback used in dev only)
# Gerar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=your-random-secret-here-min-32-chars

# AI (Frank / LLM defaults)
DEFAULT_LMSTUDIO_BASE_URL=
DEFAULT_LMSTUDIO_MODEL=
DEFAULT_EMBED_MODEL=
OPENAI_API_KEY=
DEFAULT_CLOUD_MODEL=
DEFAULT_CLOUD_BASE_URL=https://api.openai.com/v1
DEFAULT_AI_TIMEOUT_MS=

# VECTOR DB (Qdrant local dev)
QDRANT_URL=http://127.0.0.1:6333
QDRANT_COLLECTION=condstore_docs
QDRANT_API_KEY=

# RAG (default OFF)
RAG_ENABLED_DEFAULT=false
RAG_ENABLED_TENANTS=
RAG_MAX_CHUNKS=5
RAG_MAX_CONTEXT_CHARS=1500
RAG_MIN_SCORE=0.75
RAG_DOCS_MAX_CHUNKS=3
RAG_CHAT_MAX_CHUNKS=2
RAG_DOCS_MIN_SCORE=0.75
RAG_CHAT_MIN_SCORE=0.75
RAG_MIN_AVG_SCORE=0.78

# Redis (Workers)
REDIS_URL=redis://localhost:6379

# Internal Jobs Auth
INTERNAL_JOB_TOKEN=your-random-token-here

# ──────────────────────────────────────────────────────────────
# Stripe (Billing Core v1)
# ──────────────────────────────────────────────────────────────
# 0 = manual upgrade flow  |  1 = Stripe Checkout redirect
NEXT_PUBLIC_STRIPE_ENABLED=0

# Stripe secret key (sk_live_... or sk_test_...)
STRIPE_SECRET_KEY=

# Stripe webhook signing secret (from `stripe listen` or Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET=

# Stripe Price IDs for each paid plan (from Dashboard > Products)
STRIPE_PRICE_GROWTH=    # $49/month – plan_growth
STRIPE_PRICE_PRO=       # $99/month – plan_pro
STRIPE_PRICE_SCALE=     # $299/month – plan_scale
